from setuptools import setup
# from version import get_version
setup(
    name='pysleep',
    version='0.1',
    description='Python package for Learning From Sleep data project.',
    packages=['pysleep'],
    install_requires=[
        'numpy',
    ],
)